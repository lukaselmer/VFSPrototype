<Window x:Class="VFSBrowser.View.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:view="clr-namespace:VFSBrowser.View"
        Title="InputDialog" MinHeight="125" MinWidth="309" Height="125" Width="309">

    <StackPanel>
        <Label Content="{Binding Path=Label, FallbackValue=Input}"></Label>
        <TextBox Name="TextBox" Text="{Binding Text}" Margin="0 0 0 5">
            <TextBox.InputBindings>
                <KeyBinding Key="Enter" Command="{Binding Path=OkCommand}" />
            </TextBox.InputBindings>
        </TextBox>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Margin="5" Width="75" Name="OkButton" Command="{Binding Path=OkCommand}" Content="OK" />
            <Button Margin="5" Width="75" IsCancel="True" Content="Cancel" />
        </StackPanel>
    </StackPanel>
</Window>
